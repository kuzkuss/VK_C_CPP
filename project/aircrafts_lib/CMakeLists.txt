cmake_minimum_required(VERSION 3.14)
project(aircrafts_lib)

file(GLOB SOURCES *.c)
file(GLOB INCLUDE_DIRS ${CMAKE_CURRENT_SOURCE_DIR})

add_library(${PROJECT_NAME} ${SOURCES})

set(AIRCRAFTS_LIB_LIBRARIES ${PROJECT_NAME} PARENT_SCOPE)
set(AIRCRAFTS_LIB_INCLUDE_DIRS ${INCLUDE_DIRS} PARENT_SCOPE)

